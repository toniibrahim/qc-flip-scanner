# QC Flip Scanner — Daily Scan 2026-05-17

> **⚠ NO LIVE-DATA CONNECTOR ATTACHED — Firecrawl MCP not configured on this routine**
>
> The Firecrawl MCP connector required by §0 of the skill is not available in this execution environment.
> Per §0, the scan cannot proceed: `web_search` and `web_fetch` are explicitly forbidden substitutes.
> This file is committed to document that the routine fired and the abort condition was correctly enforced.

---

## Section A — Deal Table

| Address | Source | Status | Asking Price | Target Buy Price | Reno Level | Est. Reno Cost | ARV Conservative | ARV Target | ARV Optimistic | Gross Profit | Net Profit | Grade | Main Risk | Recommendation |
|---|---|---|---:|---:|---|---:|---:|---:|---:|---:|---:|---|---|---|
| NO LIVE-DATA CONNECTOR ATTACHED — Firecrawl MCP not configured on this routine | — | — | — | — | — | — | — | — | — | — | — | — | — | — |

---

## Section B — Per-Property Notes

No candidates. The Firecrawl MCP connector required by §0 was not available in this session. No listing pages were fetched, no ARV was estimated, and no deals were graded.

---

## §12 Mandatory Data Quality Disclosure

**Scan timestamp:** 2026-05-17  
**Trigger source:** Scheduled

### Pre-Run Connector Verification (§0 Required Check)

Per §0, the routine must confirm that `firecrawl_search` and `firecrawl_scrape` are available before
starting Step 1. This check was performed via `ToolSearch` at session start.

**Result: FAIL — Neither `firecrawl_search` nor `firecrawl_scrape` was found in the available tool list.**

The Firecrawl skill file at `.claude/skills/firecrawl/firecrawl.md` is present in the repo but provides
only documentation — it does not inject MCP tools into the session. The Firecrawl MCP *server* must be
configured in the Claude Code on the Web environment for these tools to appear.

### Tools Called This Run

| Tool | Calls | Outcome |
|---|---|---|
| `ToolSearch` (connector verification) | 1 | Confirmed: Firecrawl MCP not present in session |
| `firecrawl_search` | 0 | Not available — tool absent |
| `firecrawl_scrape` | 0 | Not available — tool absent |
| `web_search` | 0 | Explicitly forbidden by §0 |
| `web_fetch` | 0 | Explicitly forbidden by §0 |

### Sources Reached

None. No web data was fetched during this run.

### Per-Candidate Data Quality

No candidates. Scan aborted at §0 pre-condition check before Step 1.

### Scan-Level Assessment

**NO LIVE-DATA CONNECTOR ATTACHED.** The Firecrawl MCP connector is required for all listing and
comparable data in this skill. Without it, the routine cannot fetch Centris, REALTOR.ca, DuProprio, or
any major Quebec residential portal — all of which return HTTP 403 to raw web fetchers. The run aborted
correctly before Step 1 per §0 operating rules.

**How to fix for future runs:**  
Configure the Firecrawl MCP server in the Claude Code on the Web environment for the
`toniibrahim/qc-flip-scanner` repository. Once configured, `Firecrawl_MCP:firecrawl_search` and
`Firecrawl_MCP:firecrawl_scrape` will appear in the session tool list and the §0 pre-check will pass.

---

*Generated by qc-flip-scanner routine · Trigger: scheduled · Firecrawl calls: 0 · Repo: https://github.com/toniibrahim/qc-flip-scanner*
